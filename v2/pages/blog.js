import { Provider, Heading, Button } from "rebass";

import Card from "../components/Card";

const Blog = () => {
    return (
        <Provider>
            <Heading>Blog</Heading>
            <Heading>How much advice you need?</Heading>
        </Provider>
    );
};

export default Blog;
